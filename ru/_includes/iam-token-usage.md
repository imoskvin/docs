Полученный IAM-токен указывайте при обращении к ресурсам Яндекс.Облака через API. Передайте IAM-токен в заголовке `X-YaCloud-SubjectToken` в следующем формате:

```
X-YaCloud-SubjectToken: <IAM-TOKEN>
```
